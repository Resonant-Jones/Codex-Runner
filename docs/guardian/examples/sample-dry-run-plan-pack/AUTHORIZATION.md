# Guardian Authorization

## Authorized Mode

- `authorized_mode`: `simulated dry-run planning only`
- `execute_authorized`: `no`

Only Chris may approve a transition from `dry_run` to `execute` for a specific run or task. This sample does not grant that approval.

## Authorized Authority Level

- `authority_level`: `Guardian Operating, CLI/docs-only example layer`
- `effective_from`: `example preparation only`
- `effective_until`: `end of this simulated planning example`

Authority is fixed, explicit, and not self-promotable.

## Allowed Command Classes

- Read-only inspection commands for docs discoverability
- Example references to Codex Runner dry-run and receipt-report commands
- Session-log drafting inside the sample plan-pack area

## Allowed File Classes

- Example plan-pack files
- Example task spec files
- Read-only docs referenced by the sample scenario
- Simulated session-log files inside this example directory

## Forbidden Authority Changes

- Self-promotion from Level 1 to any higher authority
- Treating this example as active permission to execute a loop
- Implied authorization from successful validation or exit code `0`
- Treating `candidate` as approval or ingestion
- Treating reports as permission
- Treating receipts as durable truth

## Expiration Or Session Boundary

- `session_boundary`: `this sample ends at documentation and planning preparation`
- `approval_expires_at`: `immediately after the example is reviewed`
- `revalidation_required_if`: `any real execution, broader scope, or cross-repo authority is requested`

## Human Approval Notes

- Chris approval note: approved as a sample bowl only
- Scope approved: example plan-pack preparation for a harmless docs discoverability review
- Mode approved: simulated dry-run planning only
- Known exceptions: none

## Signature Block

- `approved_by`: Chris
- `approved_at`: `<fill when using this example as a walkthrough>`
- `approval_reference`: `sample only - not an active authorization`

If this file is unsigned or incomplete, Guardian must not assume authority. Even if signed, this sample still does not authorize execution beyond docs-only planning.
